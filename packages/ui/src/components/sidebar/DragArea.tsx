import DraggableArea from '../draggable-area';
import styles from './sidebar.module.css';

const DragArea = () => {
  return <DraggableArea className={styles.drag_area} />;
};

export default DragArea;
